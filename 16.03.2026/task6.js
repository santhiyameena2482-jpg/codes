//  const colors = ["Red", "Blue"];
//    colors.push("Green");
//    console.log(colors);
   
 const colors = ["Red", "Blue"];
   colors.push("Green");
   colors=["Yellow"];
   console.log(colors);
   
   //first one is work because it only add the element to array not change its entire array .but second one change whole array value, so it gives TypeError that means array only store reference.
