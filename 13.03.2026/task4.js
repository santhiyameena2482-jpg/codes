console.log([] == false) // empty array=false , so false == false => true.
console.log([] === false)// === operator check both values and datatype ,
// array is non-primitive datatype and false is boolean type so, reference === value => false.
//  so it gives false.